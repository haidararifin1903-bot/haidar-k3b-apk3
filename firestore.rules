rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // HAI CONTAINER - Terminal Operating System rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
